# MiraNote — AI Coding Rules

Canonical entry point for AI coding assistants (Claude Code) working in any
`MiraNote-AI/*` repo. The full rule set lives in [`docs/ai/`](docs/ai/).

> **Source of truth:** `MiraNote-AI/.github`. Edit here only.
> The `sync-ai-docs.yml` workflow propagates changes to every code repo via PR.

## Quick rules

_To be filled in via brainstorm — see [`docs/ai/README.md`](docs/ai/README.md)._
